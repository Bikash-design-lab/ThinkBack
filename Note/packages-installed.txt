pip install "pymongo[async]"

1. What yield does in Python?
👉 Yield is used to return a value from a function one by one.
👉 It is a special kind of function generator. 
👉Saves memory (good for big data)
👉 Can pause and continue the function
👉 Used in loops, streaming, reading big files
-> Normal function (returns the whole list at once | executes function immediately)
    def nums():
        return [1,2,3]
-> Using yield(sends once at a time | executes function one at a time)
    def nums():
        yield 1
        yield 2
        yield 3
Explanation: This sends:
first → 1
next → 2
next → 3
One at a time.

2. What raise does in Python?
👉 raise is like error stop and return is like normal stop.
👉 Raise is used to throw an error manually.
👉 Stop the program here and show this error”
-> age = -5
    if age < 0:
        raise ValueError("Age cannot be negative")
    else:
        print("Age is valid")


3. What is asyncio in Python?
👉 Asyncio is a library for writing concurrent code using the async/await syntax.
    def main():
        print("Hello")
        await asyncio.sleep(1)
        print("World")
    asyncio.run(main())

4. What is async def in Python?
    def main():
        print("Hello")
        await asyncio.sleep(1)
        print("World")
    asyncio.run(main())

